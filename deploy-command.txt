# Copy and paste this command in Google Cloud Shell:

# 1. First, clone or upload your code to Cloud Shell
# 2. Then run this command:

gcloud run deploy carrier-api --source . --platform managed --region us-central1 --allow-unauthenticated --memory=512Mi --cpu=1 --max-instances=10 --timeout=300

# Alternative if you want to specify the project explicitly:
# gcloud run deploy carrier-api --source . --platform managed --region us-central1 --allow-unauthenticated --project=YOUR_PROJECT_ID

# After deployment, test with:
curl -X POST https://carrier-api-834528330838.us-central1.run.app/api/verify-carrier -H "Content-Type: application/json" -H "X-API-Key: ak_verify_1234567890abcdef" -d '{"mc_number": "MC-441100", "use_fmcsa": false}'
